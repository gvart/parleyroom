ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [ com.gvart.parleyroom.ApplicationKt.module ]
  }
}

database {
  url = "jdbc:postgresql://localhost:5432/parleyroom"
  url = ${?DB_URL}
  user = "parleyroom"
  user = ${?DB_USER}
  password = "parleyroom"
  password = ${?DB_PASSWORD}
}

jwt {
  secret = "secret"
  issuer = "parley-room"
  audience = "parley-room-api"
  realm = "ParleyRoomRealm"
  duration = "60m"
}

livekit {
  url = "ws://localhost:7880"
  url = ${?LIVEKIT_URL}
  api_key = "devkey"
  api_key = ${?LIVEKIT_API_KEY}
  api_secret = "devsecretdevsecretdevsecretdevsecret"
  api_secret = ${?LIVEKIT_API_SECRET}
  token_ttl = "2h"
  token_ttl = ${?LIVEKIT_TOKEN_TTL}
}


application {
  admin {
    default_password = "admin"
    default_password = ${?APPLICATION_ADMIN_DEFAULT_PASSWORD}
  }
}

storage {
  endpoint = "http://localhost:9000"
  endpoint = ${?STORAGE_ENDPOINT}
  region = "us-east-1"
  region = ${?STORAGE_REGION}
  access_key = "minioadmin"
  access_key = ${?STORAGE_ACCESS_KEY}
  secret_key = "minioadmin"
  secret_key = ${?STORAGE_SECRET_KEY}
  bucket = "parleyroom-materials"
  bucket = ${?STORAGE_BUCKET}
  upload_url_ttl = "15m"
  upload_url_ttl = ${?STORAGE_UPLOAD_URL_TTL}
  download_url_ttl = "1h"
  download_url_ttl = ${?STORAGE_DOWNLOAD_URL_TTL}
  path_style_access = true
  path_style_access = ${?STORAGE_PATH_STYLE}
}